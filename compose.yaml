services:
  master-relax:
    container_name: master-relax-app
    env_file:
      - ./master-relax-app/.env.production
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://localhost:8080/api/health').then(r => {if(!r.ok) process.exit(1)}).catch(() => process.exit(1))",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
    image: bering152/master-relax-app-production:latest
    restart: on-failure
    ports:
      - 127.0.0.1:3010:8080
  db:
    container_name: master-relax-database
    restart: unless-stopped
    env_file:
      - ./master-relax-database/.env.production
    volumes:
      - db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    image: postgres:15
    ports:
      - 127.0.0.1:5432:5432

volumes:
  db-data:
    name: master-relax-db-data
